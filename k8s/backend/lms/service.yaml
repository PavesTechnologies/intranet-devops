apiVersion: v1
kind: Service
metadata:
  name: lms-service
  namespace: backend
  labels:
    app: lms
    team: paves
spec:
  selector:
    app: lms
  ports:
    - port: 9999
      targetPort: 9999
  type: ClusterIP
