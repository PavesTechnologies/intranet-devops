apiVersion: v1
kind: Service
metadata:
  name: rms-service
  namespace: backend
  labels:
    app: rms
    team: paves
spec:
  selector:
    app: rms
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
