apiVersion: v1
kind: Service
metadata:
  name: xms-service
  namespace: backend
  labels:
    app: xms
    team: paves
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      name: http
  selector:
    app: xms
